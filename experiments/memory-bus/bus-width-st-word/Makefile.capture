
include $(UAS_ROOT)/Makefile.common

CAPTURE_TARGETS = 

$(eval $(call tgt_ttest_capture,-1-0,--set idx=1 off=0 --zero-fixed))
$(eval $(call tgt_ttest_capture,-1-1,--set idx=1 off=1 --zero-fixed))
$(eval $(call tgt_ttest_capture,-1-2,--set idx=1 off=2 --zero-fixed))

CAPTURE_TARGETS += $(call map_ttest_capture_tgt,-1-0)
CAPTURE_TARGETS += $(call map_ttest_capture_tgt,-1-1)
CAPTURE_TARGETS += $(call map_ttest_capture_tgt,-1-2)

capture: $(CAPTURE_TARGETS)


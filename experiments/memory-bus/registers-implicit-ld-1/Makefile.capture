
include $(UAS_ROOT)/Makefile.common
include $(UAS_EXPERIMENT_SRC)/Makefile.common

#$(eval $(call add_capture_target,16,16))
#$(eval $(call add_capture_target,16,19))
#$(eval $(call add_capture_target,16,20))
$(eval $(call add_capture_target,16,64))

capture: $(CAPTURE_TARGETS)

